export interface User {
    id: number;
    firstName: string;
    lastName: string;
    phoneNumber: string;
    email: string;
}

export interface newUser {
    firstName: string;
    lastName: string;
    phoneNumber: string;
    email: string;
}