export interface User {
    id: number;
    firstName: string;
    lastName: string;
    mobileNumber: string;
    email: string;
}

export interface newUser {
    firstName: string;
    lastName: string;
    mobileNumber: string;
    email: string;
}