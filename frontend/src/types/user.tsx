// User interface
export interface User {
    id: number;
    firstName: string;
    lastName: string;
    mobileNumber: string;
    email: string;
}

// New user interface
export interface newUser {
    firstName: string;
    lastName: string;
    mobileNumber: string;
    email: string;
}